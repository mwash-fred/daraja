server:
  port: 9001

spring:
  application:
    name: MPESA-SERVICE

  jpa:
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mariadb://localhost:3306/fortunecredit
    username: root
    password: me@2020
    driverClassName: org.mariadb.jdbc.Driver

eureka:
  client:
    enabled: false
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

  instance:
    hostname: localhost

app:
  consumer:
    key: F7STco32FzELmkShGfs8GXBrsrDxEfti
    secret: YUR1qwdi8HLZJo6F